<ul class="list-group">
	<% @posts.each do |post| %>
			<li class="list-group-item"><h3><%= post.title %></h3>
			<%= link_to "View", user_post_path(current_user.id, post.id) %><br/>
			<b>View count: <%= post.post_viewers.count %></b>
			</li><br/>
	<% end %>
</ul>